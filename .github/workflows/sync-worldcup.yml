<<<<<<< HEAD
name: Sync World Cup data
=======
name: Sync World Cup
>>>>>>> 172c63a (add world cup sync api route)

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  sync-worldcup:
    runs-on: ubuntu-latest
<<<<<<< HEAD

    steps:
      - name: Call Cup Clash sync endpoint
        run: |
          curl --fail --show-error --silent \
            -H "Authorization: Bearer ${{ secrets.WORLD_CUP_SYNC_SECRET }}" \
            "https://cup-clash-rose.vercel.app/api/cron/sync-worldcup"
=======
    timeout-minutes: 5
    steps:
      - name: Trigger World Cup sync
        env:
          WORLD_CUP_SYNC_SECRET: ${{ secrets.WORLD_CUP_SYNC_SECRET }}
        run: |
          curl --fail --show-error --silent \
            --header "Authorization: Bearer ${WORLD_CUP_SYNC_SECRET}" \
            https://cup-clash-rose.vercel.app/api/cron/sync-worldcup
>>>>>>> 172c63a (add world cup sync api route)
